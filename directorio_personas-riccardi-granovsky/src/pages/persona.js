import * as React from 'react';
import { View, Text } from 'react-native';

function PersonaScreen(navigation) {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
    </View>
  );
}